#include <stdio.h>

int main() {
    int i, n = 0;
    int a[20], par[20];
    count = 0;

    printf("Entre com o número de elementos: ");
    scanf("%d\n", &n);

    for(i = 0; i < n; i++) {
        scanf("%d\n", &a[i]);
        
    }

    printf("a[%d] = %d\n", i, a[i]);
    return 0;
}